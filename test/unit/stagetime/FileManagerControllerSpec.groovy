package stagetime
/**
 * See the API for {@link grails.test.mixin.web.ControllerUnitTestMixin} for usage instructions
 */
/*
@TestFor(FileManagerController)
@Mock([SessionService, GeneralService])
class FileManagerControllerSpec extends Specification {

    def setup() {
    }

    def cleanup() {
    }

    void "test exists action"() {
        given:
        SessionService sessionService = Mock()
        sessionService.getUser() >> 2
        GeneralService generalService = Mock()

        //GeneralService.fileExists()

        and:
        1 * generalService.fileExists() >> true


        expect:
    }
}
*/